#include <bits/stdc++.h>
using namespace std;

bool helper(int n, int a[]) {}

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, x;
        cin >> n >> x;
        cout << pow(2, x - n) << endl;
    }
    return 0;
}